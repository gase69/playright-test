# AWS RDS Pricing Calculator Estimate Generator (Python)

Standalone Python CLI generator for AWS RDS Pricing Calculator estimates built with Playwright, Typer, Pydantic, and Rich.

## Usage

```bash
uv run generate-aws-rds-estimate-py [options]
```
